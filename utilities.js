
function random(){
    const names = ['Jason', 'Aaron', 'Charlie', 'Lola', 'John', 'Ida', 'Kamran', 'Eddie', 'Lila', 'Romeo'];
    const number = Math.floor(Math.random() * 11);
    const name = names[number];

    alert(name)
}
